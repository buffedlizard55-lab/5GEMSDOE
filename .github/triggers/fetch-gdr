# Touch this file (append a line) and push an arena/** branch to fire
# .github/workflows/fetch-gdr-inventory.yml, which downloads, verifies and commits the
# GDR 355 Great Basin geothermal structural inventory (DOI 10.15121/1148722, CC BY 4.0).
# gdr.openei.org is unreachable from the dev sandbox (curl exit 35, verified 2026-09-25),
# and `gh workflow run` is refused with 403, so a push-path trigger is the only channel.
# session 5GEMSDOE-3, 2026-09-25: switch created.
# session 5GEMSDOE-3, re-armed after the empty-inputs failure.
